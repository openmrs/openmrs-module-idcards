README

This directory contains those jars that are <b>not</b> included in the OpenMRS api/jar already. 

If you need to include a jar that openmrs has in its lib directory already, use the lib-common directory